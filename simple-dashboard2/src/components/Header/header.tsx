import Box from "@mui/material/Box";

const Header = () => {
  return (
    <Box display={"inline-block"}>
      <h3 style={{ paddingLeft: 10, color: "#b8bcc8" }}>BUDGET DASHBOARD</h3>
    </Box>
  );
};

export default Header;
